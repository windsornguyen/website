# Database environment configuration.
# Local uses supabase CLI; remote envs use SUPABASE_DB_URL.

environments:
  local:
    project_id: website
    api_port: 54321
    db_port: 54322

  preview:
    # Wired via SUPABASE_DB_URL in preview environments.
    # Set SUPABASE_DB_URL in the deployment provider or local env.

  production:
    project_ref: eqxmqnhoupocncjcbwme
    # Wired via SUPABASE_DB_URL in production environments.
    # Set SUPABASE_DB_URL in the deployment provider or local env.
